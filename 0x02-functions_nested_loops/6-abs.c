#include "main.h"
#include <stdio.h>
/**
 * abs - compute the absolute value of an interger
 * @t: the interger to be computed
 * Return: the absolute value
 *
 */
int _abs(int t);
{
	int k = 'a';
	int absvalue;

	if (k < 0)
		k = k * (-1);
	absvalue = k;
	return (absvalue);
}
