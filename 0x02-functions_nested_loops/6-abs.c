#include "main.h"
/**
 * abs - compute the absolute value of an interger
 * @t: the interger to be computed
 * Return: the absolute value
 */
int _abs(int t);
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
