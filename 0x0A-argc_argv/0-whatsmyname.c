#include <stdio.h>
/**
 * main - function to print it's name
 * @argc: argc parameter
 * @argv: an array of a command listed
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", *argv[])
		return (0);
}
