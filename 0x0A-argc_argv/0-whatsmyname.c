#include <stdio.h>
/**
 * main - print the name of the program
 * @argc: number of argument pass through the function
 * @argv: argument vector from pointer to string
 *
 * Return: 0
 */
int main(argc_attribute_((unused)). char *argv[])
{
	printf("%s\n" argv[])
		return (0);
}
